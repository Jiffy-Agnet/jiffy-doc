---
title: Comparison
description: How Jiffy's self-hosted, thin-Gateway architecture differs from generic CI bots, hosted AI coding agents, and specific tools like Claude Code, Kilo Code, and Codex.
---

import { Callout } from 'fumadocs-ui/components/callout';

Jiffy sits at the intersection of two categories of tooling — CI/automation bots and AI coding agents — without being a straightforward member of either. This page is a qualitative look at where it differs, not a benchmark; it doesn't claim to be faster, cheaper, or more accurate than any specific product.

## Versus generic CI bots

CI bots (lint-fixers, dependency-update bots, label automations) are good at fast, mechanical, well-defined changes triggered by rules. What they generally don't do is read an open-ended Issue thread — a human description of a problem — and write the actual fix.

- **Task input**: a CI bot reacts to structured events (a new dependency version, a failing check). Jiffy reacts to a natural-language Issue thread — the same place a human would describe the task to a teammate.
- **Output**: a CI bot's change is usually a small, mechanical diff. Jiffy's output is a full implementation attempt, arrived at through the same clone → understand → change → commit → push → PR flow a developer would follow.
- **Where it runs**: most CI bots run as part of your existing CI provider's hosted runners. Jiffy's Gateway and sandbox are infrastructure you deploy and control yourself.

## Versus hosted AI coding agents

A number of AI coding agents are offered as a managed, hosted service: you grant the vendor's platform access to your repository, and it runs the agent on infrastructure you don't control.

- **Self-hosted by default** — Jiffy's Gateway and sandbox run on your own infrastructure. Your repository, Issue content, and task history never pass through a third-party platform unless you choose to connect a third-party model backend as the agent's LLM.
- **Thin-Gateway, edge-filtered architecture** — mention detection happens at the edge, inside your own CI (or a relay you run), before anything reaches your Gateway. A hosted agent typically has its platform watching your repository directly; Jiffy's Gateway only ever sees requests that already passed a same-repo validity check.
- **Provider-neutral by construction** — because provider-specific logic lives entirely in the Edge Component, GitHub, GitLab, and Gitea are equally first-class. Many hosted agents are built around one platform's integration surface first.
- **You own the execution environment** — the sandbox's image, resource limits, and installed toolchains are yours to configure and audit, rather than an opaque part of a vendor's platform.

<Callout type="info">
None of this makes Jiffy a drop-in replacement for a fully managed product — you're taking on running the Gateway and sandbox yourself. The trade is control and data locality in exchange for operational responsibility.
</Callout>

## Versus specific coding agents

The comparisons above are about categories of tooling. In practice, most teams are actually weighing Jiffy against one or two named products, so here's a more concrete look at three widely used coding agents: Claude Code (Anthropic's coding agent, typically run as a GitHub Action), Kilo Code (an open-source, multi-model coding agent), and Codex (OpenAI's coding agent).

| Dimension | Jiffy | Claude Code | Kilo Code | Codex |
|---|---|---|---|---|
| Git providers | GitHub, GitLab, and Gitea, equally supported | GitHub, via its official GitHub Action | GitHub and GitLab, through its cloud-agent feature | GitHub only |
| Where the agent runs | Fully on your own infrastructure — Gateway and sandbox both | Inside a GitHub Actions runner; the model call itself goes to a hosted provider | The core agent can run entirely on your machine; its cloud-agent feature runs on hosted infrastructure | OpenAI's hosted cloud, or a runner that calls OpenAI's API |
| Model choice | Model-agnostic — you configure which model powers the agent | Built around Anthropic's models, extensible to a few compatible cloud providers | Model-agnostic — bring your own key across many providers | Tied to OpenAI's own models |
| How a task starts | An `@jiffy` mention in an Issue thread, filtered at the edge before it ever reaches the Gateway | An `@claude` mention in an Issue or pull request comment | A task assigned from the editor, the CLI, or a connected repository | An issue, a label, or a task started from a connected app |
| Running several tasks at once | A worker pool picks up queued tasks, each in its own isolated container | Limited by how many Actions jobs your runners can run in parallel | Cloud agents can run in parallel, each in its own isolated workspace | Cloud tasks can run in parallel, each in its own isolated environment |
| Cost model | Open-source; you pay only for your own infrastructure and whichever model you choose | Free action, but usage draws from your Anthropic usage budget | Open-source; usage-based cost from whichever model you bring | Requires a ChatGPT subscription plan |

### Feature checklist

The same comparison, as a flat checklist. A feature counts as supported (✅) only if it works the way described out of the box, without a third-party plugin or workaround the vendor doesn't officially support:

| Feature | Jiffy | Claude Code | Kilo Code | Codex |
|---|---|---|---|---|
| GitHub Issues | ✅ | ✅ | ✅ | ✅ |
| GitLab Issues | ✅ | ❌ | ✅ | ❌ |
| Gitea Issues | ✅ | ❌ | ❌ | ❌ |
| Whole pipeline self-hosted on your own infrastructure | ✅ | ❌ | ❌ | ❌ |
| Model-agnostic — bring your own model or provider | ✅ | ❌ | ✅ | ❌ |
| Open-source agent | ✅ | ❌ | ✅ | ✅ |
| Task starts from an `@mention` in an Issue thread | ✅ | ✅ | ❌ | ❌ |
| Opens the Pull Request itself | ✅ | ✅ | ✅ | ✅ |

<Callout type="info">
This space moves fast — treat both tables as a snapshot rather than a permanent feature matrix, and check each product's own documentation for anything you're relying on.
</Callout>

## Where Jiffy fits

Jiffy is a good fit for teams that already default to Issue-driven workflows, want the agent's execution environment on their own infrastructure, and need GitHub, GitLab, and Gitea to be equally supported rather than one primary platform with others as an afterthought. It's not built to replace a full CI pipeline.
